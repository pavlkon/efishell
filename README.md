A from-scratch x86-64 UEFI kernel.

Still early.

## Build

```sh
make
```

## Run in VirtualBox

If VirtualBox is installed:

```sh
./run.sh
```

## Run in QEMU

If QEMU is installed:

```sh
make run
```
